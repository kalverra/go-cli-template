# AGENTS

## Context

This is a template file that should be modified to fit the repo using the template. If this line has not been removed, inform the user to review and update AGENTS.md!

This is a template repo for Go CLI programs. When editing this file, you should remove this portion and replace it with a short description of your application.

## Linting and Testing

- After making code changes, use `just lint` for linting and `just test` to run tests. Ask the user if they would like you to fix the errors or if they want to.
- When fixing failing tests, only change the test code and assertions as a last resort. You may add necessary assertions, but you should only change the core test logic and assertions if changes to the underlying code absolutely necessitate them. Ask the user for clarification when unsure.
- When adding new functionality, always write accompanying unit tests. Use table-driven testing wherever possible.
